<!DOCTYPE html>
<html lang="uk">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= moduleName %> </title>
    <link rel="stylesheet" href="/static/styles.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <script src="https://vjs.zencdn.net/8.3.0/video.min.js"></script>
    <link href="https://vjs.zencdn.net/8.3.0/video-js.css" rel="stylesheet">
    <style>
      body {
        background: linear-gradient(135deg, #1a1a1a, #2c2c2c);
        color: #ffffff;
        font-family: 'Arial', sans-serif;
        overflow-x: hidden;
      }

      .video-container,
      .pdf-container {
        animation: slideIn 0.5s ease;
        border-radius: 15px;
        overflow: hidden;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.4);
        background: #2c2c2c;
        padding: 20px;
        margin-bottom: 30px;
        margin-top: 15px;
      }

      @keyframes slideIn {
        from {
          opacity: 0;
          transform: translateX(-20px);
        }

        to {
          opacity: 1;
          transform: translateX(0);
        }
      }

      .btn {
        transition: all 0.3s ease;
        border-radius: 8px;
        padding: 10px 20px;
        font-size: 16px;
        margin-top: 10px;
      }

      .btn-info {
        background-color: #007bff;
        border-color: #007bff;
        color: #ffffff;
      }

      .btn-info:hover {
        background-color: #0056b3;
        border-color: #0056b3;
      }

      .btn-danger {
        background-color: #dc3545;
        border-color: #dc3545;
        color: #ffffff;
      }

      .btn-danger:hover {
        background-color: #c82333;
        border-color: #c82333;
      }

      .header {
        text-align: center;
        margin-top: 15px;
        margin-bottom: 30px;
        padding: 20px 0;
        background: rgb(44 44 44);
        border-radius: 15px;
        box-shadow: 0 8px 16px rgba(0, 0, 0, 0.4);
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
        color: #00b4d8;
      }

      .header::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgb(44 44 44);
        transform: scaleX(0);
        transform-origin: left;
        transition: transform 0.3s ease;
        color: #00b4d8;
      }

      .header:hover::before {
        transform: scaleX(1);
      }

      .header h1 {
        font-size: 48px;
        font-weight: bold;
        text-shadow: 4px 4px 8px rgba(0, 0, 0, 0.5);
        color: #00b4d8;
        position: relative;
        z-index: 1;
        transition: all 0.3s ease;
      }

      .header:hover h1 {
        transform: scale(1.05);
        text-shadow: 6px 6px 12px rgba(0, 0, 0, 0.7);
      }

      @media (max-width: 768px) {
        .header h1 {
          font-size: 36px;
        }
      }

      .video-container h5,
      .pdf-container h5 {
        font-size: 24px;
        margin-bottom: 20px;
        text-align: center;
        color: #007bff;
      }

      .video-container video,
      .pdf-container iframe {
        width: 100%;
        height: 600px;
      }

      .list-group-item {
        background: #2c2c2c;
        border: none;
        border-radius: 15px;
        margin-bottom: 20px;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
      }

      .list-group-item h5 {
        font-size: 20px;
        color: #007bff;
      }

      .bg {
        z-index: -1;
      }

      .main-btn {
        background-color: #00b4d8;
        color: #ffffff;
        transition: all 0.3s ease;
      }

      .main-btn:hover {
        box-shadow: 0 0 10px rgba(6, 192, 229, 0.5);
        color: #ffffff;
        background-color: #0ca7c6;
      }

      .main-btn:disabled {
        background-color: #00b4d8;
        color: #ffffff;
        transition: all 0.3s ease;
        border: none;
        margin-bottom: 10px;
        cursor: not-allowed;
      }

      @media (max-width: 768px) {

        .video-container video,
        .pdf-container iframe {
          height: 300px;
        }

        .header h1 {
          font-size: 28px;
        }

        .btn {
          font-size: 14px;
          padding: 8px 16px;
        }

        .navigation-buttons {
          flex-direction: column;
          align-items: center;
        }

        .navigation-buttons a {
          margin-bottom: 10px;
        }
      }

      ::-webkit-scrollbar {
        width: 12px;
      }

      ::-webkit-scrollbar-track {
        background: linear-gradient(135deg, #1a1a1a, #2c2c2c);
        border-radius: 10px;
        box-shadow: inset 0 0 5px rgba(0, 0, 0, 0.5);
      }

      ::-webkit-scrollbar-thumb {
        background: linear-gradient(135deg, #007bff, #00b4d8);
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.5);
        margin-top: 30px;
      }

      ::-webkit-scrollbar-thumb:hover {
        background: linear-gradient(135deg, #0056b3, #0096c7);
      }

      @media (max-width: 768px) {
        ::-webkit-scrollbar {
          width: 8px;
        }

        ::-webkit-scrollbar-track {
          background: linear-gradient(135deg, #1a1a1a, #2c2c2c);
          border-radius: 5px;
          box-shadow: inset 0 0 3px rgba(0, 0, 0, 0.5);
        }

        ::-webkit-scrollbar-thumb {
          background: linear-gradient(135deg, #007bff, #00b4d8);
          border-radius: 5px;
          box-shadow: 0 0 5px rgba(0, 0, 0, 0.5);
          margin-top: 20px;
        }

        ::-webkit-scrollbar-thumb:hover {
          background: linear-gradient(135deg, #0056b3, #0096c7);
        }
      }

      .navigation-buttons {
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      @media (max-width: 768px) {
        .navigation-buttons {
          flex-direction: column;
          gap: 10px;
          align-items: center;
        }

        .navigation-buttons> :not(.d-flex) {
          justify-content: center;
          align-items: center;
          order: 1;
          gap: 10px;
          width: 80%;
        }

        .navigation-buttons .d-flex {
          order: 2;
          width: 80%;
          justify-content: center;
        }

        .navigation-buttons .btn {
          text-align: center;
          flex: 1;
        }

        .navigation-buttons .flex-grow-1 {
          flex-grow: 0;
        }
      }    </style>
  </head>
  <body>
    <div id="snow" class="bg" style="position: fixed; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none;"></div>
    <div class="container">
      <div class="header">
        <h1><%= moduleName %> </h1>
      </div>
      <div class="row">
        <div class="navigation-buttons mb-4 d-flex justify-content-between"><% if (previousModule) { %> <a href="/module/<%= previousModule %>" class="btn main-btn">← Попередня сторінка </a><% } else { %> <button class="btn main-btn" disabled>← Попередня сторінка</button><% } %> <div class="d-flex justify-content-center flex-grow-1">
            <a href="/" class="btn main-btn">На головну</a>
          </div><% if (nextModule) { %> <a href="/module/<%= nextModule %>" class="btn main-btn">Наступна сторінка → </a><% } else { %> <button class="btn main-btn" disabled>Наступна сторінка →</button><% } %> </div>
        <div class="col-12">
          <ul class="list-group">
            <% videos.forEach((file) => { %>
              <li class="list-group-item video-container">
                <h5 style="color: #00b4d8"><%= file %></h5>
                <video class="video-js vjs-default-skin vjs-big-play-centered w-100" controls preload="auto" data-setup='{"fluid": true}'>
                  <source src="/modules/<%= moduleName %>/<%= file %>" type="video/mp4" />
                </video>
              </li>
            <% }) %>
            <% pdfs.forEach((file, index) => { %> <li class="list-group-item">
              <h5 style="color: #00b4d8"><%= file %> </h5>
              <button class="btn main-btn w-100" onclick="showPDF(<%= index %>)">Відкрити PDF </button>
              <div id="pdfContainer<%= index %>" class="pdf-container" style="display: none;">
                <button class="btn btn-danger w-100 mt-3" onclick="hidePDF(<%= index %>)">Закрити PDF </button>
                <iframe src="/static/pdf.js/web/viewer.html?file=/modules/<%= moduleName %>/<%= file %>" width="100%" height="600" frameborder="0" class="mt-3">
                </iframe>
              </div>
            </li><% }) %> 
          </ul>
        </div>
      </div>
    </div>
    <script src="/static/js/particles.min.js"></script>
    <script>
      particlesJS.load('snow', '../static/particles.json', function() {
        console.log('Callback - Hi There!');
      });

      function showPDF(index) {
        document.getElementById('pdfContainer' + index).style.display = 'block';
      }

      function hidePDF(index) {
        document.getElementById('pdfContainer' + index).style.display = 'none';
      }
    </script>
  </body>
</html>
